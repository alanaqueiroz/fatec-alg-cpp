#include <iostream>
using namespace std;

main()
{
	float x, y, a, b; 
    
    cout << "Escreva uma variavel inteiras para corresponder a x: ";
    cin >> x;
    cout << "Escreva uma variavel inteiras para corresponder a y: ";
    cin >> y;
    cout << "Sendo assim, X equivale a " << x << " e Y equivale a " << y; 
    
	x = a;
	y = b;
    
   	cout << "\nTrocando seus conteudos: X equivale a " << x << ", e Y equivale a " << y << ". \n";
}